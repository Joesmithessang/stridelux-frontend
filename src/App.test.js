// Placeholder — real integration tests will be added in a future phase.
// The default CRA smoke test is removed because React Router v7 (ESM)
// is not compatible with CRA 5's Jest transform configuration.
test('placeholder passes', () => {
  expect(true).toBe(true);
});
